class GoogleFitService {
    /// Mock function to simulate fetching today's step count from Google Fit.
  /// will replace this with real Google Fit code.

  Future<int> getTodaySteps() async {
    await Future.delayed(const Duration(milliseconds: 300));
    return 7423; // mock steps
  }
}
